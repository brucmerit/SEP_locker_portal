// Base URL for our Node/Express API.
// Check your terminal when you run "node app.js" or "npm run dev" - if the
// port shown there is different from 5000, update it here.
const API = 'http://localhost:5000/api';
const UPLOADS_BASE = 'http://localhost:5000'; // uploaded images are served from here, not /api
